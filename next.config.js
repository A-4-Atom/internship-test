/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["img.freepik.com", "lh3.googleusercontent.com", "static.cdntap.com"]
    }
}

module.exports = nextConfig
